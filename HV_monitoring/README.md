
# Installation

1. Add the `CAENHVWrapper-6.3/bin/x86_64/` folder to your `$LD_LIBRARY_PATH` environment variable.
2. Install pycaenhv:
```bash
cd pycaenhv
python3 -m pip install --user -U .
```

## Usage
1. Launch `rpc.py <board address>`. This starts the serving XML-RPC

